/**
 * Re-export from canonical location in infrastructure/frost/.
 * @deprecated Import from "../frost/frost-escrow-provider" instead.
 */
export {
  createFrostEscrowProvider,
  buildFrostP2PKOptions,
  type FrostEscrowConfig,
} from "../frost/frost-escrow-provider";
