# Examples

This directory is reserved for optional learning material. Each
`examples/<name>/` entry must teach one `@anchr/sdk` or `@anchr/protocol`
lesson for verifiable paid requests.

There are no maintained examples at the moment.
